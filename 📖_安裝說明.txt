================================================================================
拼豆像素化工具 - GitHub Actions 自動構建完整安裝封包
================================================================================

歡迎！本封包包含所有必要的檔案，可以直接使用。

================================================================================
📦 封包內容
================================================================================

本封包包含以下檔案和目錄：

✅ .github/workflows/
   └─ build-windows.yml          GitHub Actions 工作流配置

✅ electron/
   ├─ main.ts                    Electron 主進程
   └─ preload.ts                 Electron 預加載腳本

✅ tsconfig.electron.json        TypeScript 編譯配置

✅ package.json.reference        package.json 參考配置

✅ 📖_安裝說明.txt              本檔案

================================================================================
🚀 快速開始（5 步驟）
================================================================================

第 1 步：複製檔案到您的項目
────────────────────────────────────────────────────────────────────────────
1. 解壓本封包
2. 複製 .github/ 目錄到您的項目根目錄
3. 複製 electron/ 目錄到您的項目根目錄
4. 複製 tsconfig.electron.json 到您的項目根目錄

您的項目結構應該如下：

pixelator-app/
├── .github/
│   └── workflows/
│       └── build-windows.yml          ← 已複製
├── electron/
│   ├── main.ts                        ← 已複製
│   ├── preload.ts                     ← 已複製
│   └── dist/                          ← 自動生成
├── client/
├── server/
├── drizzle/
├── package.json                       ← 需要驗證
├── tsconfig.electron.json             ← 已複製
└── ... 其他檔案

第 2 步：驗證 package.json
────────────────────────────────────────────────────────────────────────────
1. 打開您的 package.json
2. 查找 "scripts" 部分
3. 確認包含以下命令：
   "build:electron": "vite build && tsc electron/main.ts --outDir electron/dist && tsc electron/preload.ts --outDir electron/dist && electron-builder"

4. 查找 "build" 部分
5. 確認包含以下配置：
   {
     "appId": "com.pixelator.app",
     "productName": "拼豆像素化工具",
     "directories": {
       "buildResources": "assets",
       "output": "dist-electron"
     },
     "files": [
       "dist/**/*",
       "electron/dist/**/*",
       "package.json"
     ],
     "win": {
       "target": [
         {
           "target": "nsis",
           "arch": ["x64"]
         }
       ],
       "certificateFile": null,
       "certificatePassword": null,
       "signingHashAlgorithms": ["sha256"],
       "sign": null
     },
     "nsis": {
       "oneClick": false,
       "allowToChangeInstallationDirectory": true,
       "createDesktopShortcut": true,
       "createStartMenuShortcut": true,
       "shortcutName": "拼豆像素化工具"
     }
   }

如果缺少這些配置，請參考 package.json.reference 進行修改。

第 3 步：提交到 Git
────────────────────────────────────────────────────────────────────────────
在命令提示符中執行：

cd C:\pixelator-app
git add .
git commit -m "Add GitHub Actions workflow"
git push

第 4 步：創建 GitHub 倉庫
────────────────────────────────────────────────────────────────────────────
1. 訪問 https://github.com/new
2. 倉庫名稱：pixelator-app
3. 可見性：Public
4. 創建倉庫
5. 複製倉庫 URL

第 5 步：上傳代碼到 GitHub
────────────────────────────────────────────────────────────────────────────
在命令提示符中執行：

git remote add origin https://github.com/YOUR_USERNAME/pixelator-app.git
git branch -M main
git push -u origin main

================================================================================
⏱️ 等待自動構建
================================================================================

1. 進入您的 GitHub 倉庫
2. 點擊 "Actions" 標籤
3. 看到 "Build Windows Executable" 工作流運行
4. 等待 15-20 分鐘完成

工作流完成後，您會看到綠色的 ✓ 標記。

================================================================================
📥 下載 .exe 安裝檔
================================================================================

1. 進入最新的 "Build Windows Executable" 工作流
2. 向下滾動到 "Artifacts" 部分
3. 點擊 "windows-executable" 下載
4. 解壓 ZIP 檔案
5. 雙擊 "PixelatorApp Setup 1.0.0.exe" 安裝

================================================================================
📋 檔案清單
================================================================================

本封包包含的檔案：

✅ .github/workflows/build-windows.yml
   用途：GitHub Actions 工作流配置
   大小：約 2 KB
   位置：.github/workflows/build-windows.yml

✅ electron/main.ts
   用途：Electron 主進程
   大小：約 1.2 KB
   位置：electron/main.ts

✅ electron/preload.ts
   用途：Electron 預加載腳本
   大小：約 271 B
   位置：electron/preload.ts

✅ tsconfig.electron.json
   用途：TypeScript 編譯配置
   大小：約 704 B
   位置：tsconfig.electron.json

✅ package.json.reference
   用途：package.json 參考配置
   大小：約 3.4 KB
   用途：參考或修復您的 package.json

================================================================================
⚠️ 常見問題
================================================================================

Q1：GitHub Actions 工作流不運行
A：確認 build-windows.yml 位置正確：.github/workflows/build-windows.yml

Q2：TypeScript 編譯失敗
A：確認 tsconfig.electron.json 在項目根目錄

Q3：Electron 編譯失敗
A：確認 main.ts 和 preload.ts 在 electron/ 目錄

Q4：找不到 build:electron 命令
A：參考 package.json.reference 修正您的 package.json

Q5：構建失敗，錯誤信息中包含 "signing"
A：這是正常的，我們已配置跳過代碼簽名

Q6：Artifacts 沒有生成
A：查看工作流日誌找出具體錯誤

================================================================================
✅ 檢查清單
================================================================================

在開始之前：
☐ 已解壓本封包
☐ 已複製 .github/ 目錄
☐ 已複製 electron/ 目錄
☐ 已複製 tsconfig.electron.json
☐ 已驗證 package.json 配置
☐ 已創建 GitHub 倉庫
☐ 已上傳代碼到 GitHub

構建過程中：
☐ GitHub Actions 工作流已運行
☐ 構建已完成（15-20 分鐘）
☐ Artifacts 已生成

安裝過程中：
☐ 已下載 .exe 檔案
☐ 已解壓 .exe 檔案
☐ 已安裝應用
☐ 應用可以啟動

================================================================================
📞 需要幫助？
================================================================================

1. 查看本檔案的「常見問題」部分
2. 查看 GitHub Actions 的詳細日誌
3. 檢查 package.json.reference 的配置

================================================================================
🎉 完成！
================================================================================

現在您已擁有：

✅ 完整的 GitHub Actions 工作流
✅ 所有必要的 Electron 代碼
✅ TypeScript 編譯配置
✅ package.json 參考配置

立即開始：

1. 複製所有檔案到您的項目
2. 驗證 package.json 配置
3. 提交到 Git 並推送到 GitHub
4. 等待自動構建完成
5. 下載 .exe 安裝檔
6. 安裝並享受應用

祝您使用愉快！🎨

================================================================================
